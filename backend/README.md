This is a clock in system backend endpoints built with Flask 
## Getting Started

First install dependencies

```bash
pip install -r requirements.txt

```

Run project on windows:
```
python -m flask run
```
